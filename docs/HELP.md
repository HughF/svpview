# svpview — operator's manual

Valeport SWiFT profiler acquisition and display, version 2026.08.25.

This file is generated from the help built into the program:
`svpview --help-doc > docs/HELP.md`, or `make help-doc`. Edit
`src/sv_help.c` and regenerate — do not edit this file.

## Contents

1. What this program does
2. Connecting to the instrument
3. The status strip
4. Ready to deploy
5. Live
6. Profile
7. Chart
8. Files
9. Settings
10. Instrument settings
11. Processing a cast
12. Exporting
13. Reporting depth to the winch
14. Keyboard and mouse
15. When something is wrong
16. Command line and environment

---

## What this program does

The program does three jobs: it configures a Valeport SWiFT SVP, CTD or SWiFTplus, it downloads and plots the casts the instrument has logged, and it reports each cast's achieved depth to a C-MAX Vigo winch. It replaces Valeport Ocean and VigoDepthRelay.

> **Note.** It does not control the winch. The winch is commanded from its own web interface and its physical panel, exactly as before; this program only tells it how deep the last cast reached.

Everything works offline. There is no basemap imagery, no tile server and no internet dependency anywhere, because a survey vessel usually has none of them.

---

## Connecting to the instrument

Both the USB comms cable and the Valeport Bluetooth key appear to the computer as serial ports, at 230400 baud, 8N1. Open Settings and press Connect... to see the ports the machine can offer; if the one you want is not listed, plug it in and press Rescan.

On Windows the ports are COM numbers, listed with the name Windows knows them by — the Bluetooth key reads as "Standard Serial over Bluetooth link" and the cable as a USB serial port. That difference matters: the instrument switches its GPS off while the comms cable is connected, so a cast set up over the cable will never be ready to deploy. On Linux the same ports are /dev/ttyUSB and /dev/rfcomm names, and the account must be in the dialout group to open one.

### What the link state means

| | |
|---|---|
| **Not connected** | No port is open. Nothing can be read or written. |
| **Identifying** | The port is open and the instrument is being asked what it is. |
| **Run mode** | The normal state. The instrument is logging and broadcasting status, and the program is listening. |
| **Interrupted** | The instrument is sitting at its command prompt. It logs nothing and broadcasts nothing in this state — which is why the position on the Chart page goes stale during a download. |
| **Busy** | A transfer or a settings write is in progress. The controls that would disturb it are disabled until it finishes. |

Interrupt puts the instrument at its command prompt so its card and settings can be read; Run mode sends it back to logging. Always leave it in run mode before deploying.

---

## The status strip

The strip across the top is the same on every page. Read it left to right before every cast.

| | |
|---|---|
| **SWiFT nnnnn** | The serial number of the instrument that answered on the port. |
| **Link state** | See above. |
| **Battery** | Hours remaining, from the instrument's own estimate. It turns red below 12 hours. |
| **Position** | Latitude and longitude from the status broadcast, or "no fix". |
| **Deploy flag** | The instrument's own opinion of whether it is ready. See the next section. |
| **Winch** | Whether the Vigo winch answered the last probe. Unknown until you press Probe winch. |

---

## Ready to deploy

The instrument broadcasts a status sentence every 10 seconds while it is in smart profile mode at the surface. "NOT ready to deploy" has four causes, and the page you are on will name the one that applies.

| | |
|---|---|
| **No broadcast at all** | Nothing has been heard yet. Check the instrument is in run mode and at the surface. |
| **Comms cable plugged in** | The GPS is switched off while the USB cable is connected. Unplug it and use the Bluetooth key. |
| **No GPS fix** | Move to open sky and wait. A hot fix takes under 30 seconds, a cold one a few minutes. |
| **Pressure or clock** | Pressure reads above the trigger point, or the GPS clock is stale. Power cycle the instrument with a valid fix, so it creates a new file and takes a fresh tare. |

---

## Live

Live shows what the instrument is measuring right now, plotted as it arrives, with its identity and the last file it recorded underneath.

| | |
|---|---|
| **Checksum failures** | A running count of sentences that arrived corrupt. A handful over a long session is normal on Bluetooth; a steadily climbing count means a bad cable, a dying key battery or too much distance. |

---

## Profile

Profile plots the casts held in memory — downloaded from the instrument, or opened from a file. Casts are listed on the right, newest at the bottom; click one to select it.

| | |
|---|---|
| **Velocity, Temp, Salinity, Density** | Which traces are drawn. They share the plot and each carries its own axis. |
| **Overlay all** | Draw every cast in memory at once, with the selected one picked out. This is how you see whether the water column has changed through the day. |
| **Process...** | Trim, despike, bin and thin the selected cast. |
| **Export...** | Write the selected cast out in a survey format. |

The line above the plot gives the selected cast's maximum depth, its mean sound velocity and the range of velocity and temperature it saw. The mean velocity is the number a single-value echo sounder wants.

---

## Chart

Chart is a plan view of where the casts were taken, the track the instrument has broadcast, and where it is now. There is deliberately no basemap: a graticule, a scale bar and a north arrow work at sea with no data connection.

| | |
|---|---|
| **Fit** | Set the view to contain everything there is. |
| **+ and -** | Zoom about the middle of the plot. |
| **Labels** | Draw the time against each cast marker. |
| **Follow position** | Keep the live position centred as the vessel moves. Selecting a cast from the list turns it off, so the view stays where you put it. |

### With the mouse

| | |
|---|---|
| **Drag** | Pan. |
| **Wheel** | Zoom about the pointer. |
| **Click a marker** | Select that cast. |

### What the panel is telling you

The distance and bearing are from the current position to the selected cast — the two numbers that answer "do I need to dip again here?".

A hollow position marker means the fix has stopped being refreshed: the instrument broadcasts nothing at its command prompt, so the position freezes during a download and the age is shown beside it.

> **Note.** A cast logged without a GPS fix has no position and cannot be plotted. The panel says how many are in that state rather than quietly showing fewer casts than the list holds.

Positions come from two sources of different precision, which the page states rather than blends: each cast carries the fix written into its own file header, while the live marker and the track come from the status broadcast, which is only given to four decimal places — about 11 m.

---

## Files

Files lists the instrument's memory card. Interrupt is sent for you if the instrument is logging, so listing a directory during a survey briefly stops the instrument recording.

| | |
|---|---|
| **Root** | Go to the top of the card. |
| **Refresh** | List the current directory again. |
| **Open** | Enter a directory. |
| **Download** | Fetch a .bin file and add it to the casts in memory. Progress is reported along the bottom of the window. |

A downloaded cast is held in memory only. Export it if you want it kept.

---

## Settings

Settings holds the connection, the instrument's own configuration, and the winch reporting.

| | |
|---|---|
| **Read** | Read the configuration out of the instrument. Nothing is shown until you do. |
| **Edit...** | Change it. Nothing is written until you press Apply or OK. |
| **Probe winch** | Send one probe and wait for the reply, to prove the winch can hear this machine before a cast depends on it. |
| **Report selected cast depth** | Send the selected cast's achieved depth now. Normally this is not needed — it goes automatically when a cast is downloaded — but it is the way to repeat a report the winch missed. |
| **Select adapter...** | Choose which network adapter the report goes out of. |

---

## Instrument settings

These are the instrument's own settings, written into it and kept when it is powered down. Read them first, so that what is on the screen is what is in the instrument.

| | |
|---|---|
| **Operating mode** | Smart profile logs a file per descent and is what a winch survey wants. Continuous logs everything from power-up. |
| **Profile direction** | Which half of the dip is kept. Down cast is the normal choice: the sensor leads the disturbed water on the way down. |
| **Trigger depth** | How deep the instrument must go before it starts a file. |
| **Depth increment** | The depth change between logged samples. |
| **Trigger step** | How much the pressure must change to keep the file open. |

> **Note.** The trigger step must exceed the local sea and swell. Set it below the wave height and the instrument decides the descent has stopped while it is still going down, and the file closes early.

| | |
|---|---|
| **Require GPS fix** | In continuous mode, wait for a fix before logging, so every file carries a position. |
| **Bluetooth sleep** | Allow the instrument to be woken over Bluetooth. |
| **Auto power down** | Idle minutes before the instrument switches itself off. 9999 disables it. |
| **Site information** | Free text stored in the instrument and written into the header of every file it logs. Put the survey or the vessel here. |

---

## Processing a cast

Processing is applied to the selected cast in memory, in place. The file on the card is untouched — download or open it again to get the raw profile back.

| | |
|---|---|
| **Trim** | Keep the down cast only, discarding the ascent. |
| **Despike** | Remove samples more than 3 m/s off the local median. |
| **Depth bin** | Average samples into bins of this many metres. |
| **Thin to** | Reduce to about this many points, keeping the shape. |

Zero disables a step. Bin and thin exist because some acquisition systems cap the number of points they will accept in a profile.

---

## Exporting

Export writes the selected cast, as it currently stands, to a file. The name is proposed from the cast's own timestamp and changes with the format; edit it freely.

| | |
|---|---|
| **CSV** | Every column, for a spreadsheet. |
| **Valeport .vp2** | Valeport's own format. |
| **Kongsberg .asvp** | For Kongsberg acquisition — SIS and friends. |
| **Caris .svp** | For Caris HIPS. |
| **Hypack .vel** | For Hypack. |

---

## Reporting depth to the winch

When a cast is downloaded, its achieved depth is broadcast to the Vigo winch on UDP port 8090. Vigo records it against the dive so the winch can repeat that depth, and answers with an acknowledgement, which is reported along the bottom of the window.

### Choosing the adapter

A survey computer usually has more than one network adapter, and broadcasting to everything leaves the choice of wire to the routing table — which is how a depth report leaves by the office adapter and the winch never hears it. Pick the adapter on the survey network under Settings, Select adapter.... Only connected adapters are listed.

### If the winch does not acknowledge

- Probe the winch first. No reply means the network, not the report.
- Vigo ignores a depth report if no cast has been performed in its own session, if the cast was aborted, or if the depth is outside a tenth to ten times the requested cast depth.
- Vigo ignores a report identical to the one before it, which is why each carries its own incrementing message number.

---

## Keyboard and mouse

| | |
|---|---|
| **Hover** | Rest the pointer on any control for a one-line description of what it does. |
| **F1** | Open this page. |
| **Escape** | Cancel the dialog that is open. |
| **Mouse wheel** | Zoom the chart, or scroll a list. |
| **Drag** | Pan the chart. |

### In every dialog

| | |
|---|---|
| **Cancel** | Discard the changes and close. |
| **Apply** | Commit the changes and stay open. |
| **OK** | Commit the changes and close. |

The close button in a dialog's title bar means Cancel, the same as Escape.

---

## When something is wrong

| | |
|---|---|
| **No serial ports listed** | Plug in the cable or the Bluetooth key and press Rescan. On Linux the user must be in the dialout group to open a serial port. |
| **Connected, but no status** | The instrument is at its command prompt. Press Run mode. |
| **Checksum failures climbing** | A bad cable, a flat Bluetooth key battery, or too much distance. |
| **Casts missing from the chart** | They were logged without a GPS fix. The chart panel counts them. |
| **Winch silent** | Wrong adapter, or the winch is not on this network. Probe it, then check the adapter. |
| **Winch silent on Windows only** | Windows Firewall. The first broadcast raises a prompt, and a prompt answered with Cancel — or never shown, on a machine with the firewall locked down — blocks every depth report from then on, silently. Allow svpview on the private network, or add an outbound rule for UDP 8090. This looks exactly like a winch that is not listening. |
| **Text too small or too large** | The interface scales itself to the display. Override it with the SVPVIEW_SCALE environment variable. |

The Log page holds the conversation with the instrument, sent lines marked > and received lines <. It is the first thing to look at when something behaves oddly, and the first thing to quote in a fault report.

---

## Command line and environment

| | |
|---|---|
| **--sim** | Run against a simulated instrument that speaks the real protocol. Everything in the program works, with no hardware attached. |
| **--open FILE** | Load a .bin logged file at startup. |
| **--help** | Command line summary. |
| **--help-doc** | Write this manual to standard output as Markdown. |
| **SVPVIEW_SCALE** | Override the interface scale, e.g. 2 on a HiDPI display. |

